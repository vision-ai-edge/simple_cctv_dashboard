{
  "name": "@cctv/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun --hot run src/index.ts",
    "start": "bun run src/index.ts",
    "build": "tsc -b",
    "test": "bun test src/",
    "test:cov": "bun test src/ --coverage"
  },
  "dependencies": {
    "@cctv/db": "workspace:*",
    "@cctv/shared": "workspace:*",
    "hono": "^4.6.13",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.9.0"
  }
}
